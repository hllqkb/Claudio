Phase 2: UX improvements. Do ALL of the following:

2.1 Toast Notification System:
- Create apps/web/src/stores/toastStore.ts with Zustand: toasts array, addToast(msg, type), removeToast(id)
- Create apps/web/src/components/Toast.tsx - renders toast notifications in top-right corner, auto-dismiss after 3s
- Mount ToastContainer in apps/web/src/App.tsx at the bottom of the app div
- Add toast CSS styles to global.css (glassmorphism, slide-in animation, success/error/info colors)
- Wire toasts into: playlist save success, search errors, play errors

2.2 Keyboard Shortcuts:
- Create apps/web/src/hooks/useKeyboard.ts - global keydown listener
  - Space: togglePlay (ignore if focus is on input/textarea)
  - Left/Right arrows: seek plus/minus 5 seconds
  - Up/Down arrows: volume plus/minus 5 percent
  - N: next track
  - P: previous track
  - M: toggle mute
  - S: toggle shuffle
  - R: cycle repeat mode
- Call useKeyboard() in apps/web/src/App.tsx

2.3 Loading Skeletons:
- Create apps/web/src/components/Skeleton.tsx with pulse animation
- Use in PlayerPage when nowPlaying is null
- Use in PlaylistPage during loading
- Add skeleton CSS to global.css

2.4 MiniPlayer Integration:
- Import MiniPlayer in App.tsx
- Show it at the bottom when there is a nowPlaying song AND the user is NOT on the home page (/)
- Add proper z-index and positioning CSS

IMPORTANT:
- 4-space indent, TypeScript strict
- Follow existing code patterns
- Add new CSS to end of apps/web/src/styles/global.css
- Import Zustand store using: import { create } from "zustand"
