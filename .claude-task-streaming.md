# Task: 完成 AI 对话系统的真实流式输出 + 布局修复

## 背景
我已经手动修改了以下文件，你需要验证并补全缺失部分：

### 已改动的文件（你不需要重新改，但需要检查是否正确）
1. `apps/server/src/services/claude.service.ts` — 新增了 `generatePlanStream()` 方法，用原生 `fetch` 调 Claude API `stream:true`，逐 token 回调 `onChunk`
2. `apps/server/src/routes/intent.ts` — 新增了 `POST /api/chat/stream` SSE 端点，流式返回 chunk/plan/item/done 事件
3. `apps/server/src/prompts/plan-system.md` — 重写了系统提示词，要求 AI 像真人 DJ 一样聊天，先说话再输出 JSON 计划
4. `apps/web/src/api/client.ts` — 新增了 `streamChat()` 方法，消费 SSE 流并回调
5. `apps/web/src/stores/chatStore.ts` — 新建，管理对话状态（messages, streamingText, streamingSongs, isStreaming）
6. `apps/web/src/components/ChatArea.tsx` — 重写，AI 头像在左、用户头像在右，带歌曲推荐卡片和流式光标

### 你需要完成的工作

#### 1. 验证已有改动
- 检查以上文件是否有 TypeScript 类型错误或 import 问题
- 确保 `chatStore.ts` 中 `streamingText` 在收到 JSON 开始符号 ` ``` ` 时停止显示流式文本（只显示对话部分）
- 确保 `ChatArea.tsx` 中 `ChatHint` 按钮点击后能正确调用 `useChatStore.send()`

#### 2. 改造 PlayerPage.tsx
PlayerPage 现在还在用本地的 `chatMessages` state 和 `streamingText` state。需要改成用 `chatStore`：

- 删除 PlayerPage 中的本地 `chatMessages` state
- 删除 `streamingText` state  
- 删除 `handleUserMessage`, `handleAiResponse` callback
- 改为从 `useChatStore` 读取 `messages, streamingText, isStreaming`
- 把 `<IntentInput>` 的回调改掉：发送消息时调用 `useChatStore.getState().send(text)`
- `<ChatArea>` 不再需要 props，它自己从 store 读数据
- 保留 PlayerPage 中其他逻辑不变（播放控制、搜索、队列等）

#### 3. 改造 IntentInput.tsx
当前 `IntentInput` 自己调 `api.postIntent()`，需要改成调 `useChatStore.getState().send()`：

- 删除 `handleSubmit` 中的 API 调用逻辑
- 改为直接调用 `chatStore.send(text)` 
- 删除 `onResponse` 和 `onUserMessage` props（不再需要）
- 保留 `submitting` 状态：当 `chatStore.isStreaming` 时禁用输入

#### 4. 添加 CSS 样式
在 `apps/web/src/styles/global.css` 末尾添加以下新样式（如果不存在的话）：

```css
/* 歌曲推荐卡片 */
.song-cards { ... }
.song-card { ... 带 hover 效果，glassmorphism 风格 }
.song-card-cover { 60x60px 圆角封面 }
.song-card-play-overlay { hover 时显示播放按钮 }
.song-card-reason { 推荐理由，小字，用 mint 色 }
.song-card.streaming { 入场动画 fadeSlideUp }

/* 聊天改进 */
.chat-bubble.user { flex-direction: row-reverse; 用户消息靠右 }
.chat-avatar.user { 在右侧 }
.chat-hint { 可点击的建议标签，pill 形状 }
.chat-welcome-avatar { 大图标 }
.stream-cursor { 闪烁光标 }
.chat-bubble.system-error { 红色调 }
.song-cards-loading { 加载中提示 }
```

#### 5. 确保 IntentInput 中的提示词建议
IntentInput 的 placeholder 改成 "告诉我你想听什么..."（中文），输入框样式保持不变

## 重要约束
- TypeScript strict, 4-space indent
- 不要改动 `apps/server/src/services/ncm.service.ts` 或其他未提到的服务文件
- CSS 用已有的 CSS 变量（var(--text-primary), var(--bg-glass), var(--border-mint) 等）
- 暗色主题，glassmorphism 风格
- 歌曲卡片点击后直接播放该歌曲
